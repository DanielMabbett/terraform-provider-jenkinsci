dependencies:
  override:
    - go get -d -u github.com/hashicorp/terraform
    - go get -d -u github.com/bndr/gojenkins
    - go get -d -u github.com/DanielMabbett/terraform-provider-jenkinsci/jenkinsci
    - go get -d -u github.com/hashicorp/terraform/helper/schema

test:
  pre:
    - go vet ./...
  override:
    - make test